# 接管状态（2026-07-27）

## 已完成

- 交接包已完整导入当前 Git 仓库根目录，中文文件名正常。
- 根目录与 `server/` 依赖已安装。
- 修复了交接包遗漏的主页及底部导航 PNG 图标，共 25 个。
- `npm run typecheck` 通过。
- `npm --prefix server run check` 通过。
- `npm run build:weapp` 通过。
- `npm run build:h5` 通过。

统一验证命令：

```bash
npm run check
```

## 已确认的系统边界

- 前端：Taro 4、React、TypeScript，输出微信小程序和 H5。
- 后端：Node.js、Express、PostgreSQL。
- 数据通过 `x-tenant-id` 和 `x-device-id` 隔离；预留租户为 `tangshan`、`stdu`、`lyit`。
- API 当前使用设备 ID 会话，不等同于正式微信登录。
- 订单创建已实现，但微信支付尚未接入。
- 快递状态由用户或运营方维护，不是第三方实时物流。

## 发布前阻塞项

1. 企业小程序 AppID 与 AppSecret。
2. 微信公众平台合法域名配置：`https://stardust.sale`。
3. 微信登录与服务端会话。
4. 社区文本/图片审核、举报、封禁及审计闭环。
5. 隐私协议、用户信息用途说明和相关上架材料。
6. 如需真实支付：微信支付商户号、证书与 API v3 密钥。

## 已知技术债

- `docs/qa-mobile.mjs` 与 `docs/qa-services.mjs` 已支持自动发现 macOS、Windows
  和 Linux 上的 Chrome、Edge 或 Chromium，也可通过 `QA_BROWSER` 覆盖路径。
- H5 首屏构建资源约 331 KiB，Pacifico 字体约 245 KiB，Webpack 给出体积警告。
- 当前锁定依赖的 `npm audit` 报告包含 54 项漏洞（16 moderate、29 high、9 critical），需要按生产可达性逐项评估，不能直接使用 `npm audit fix --force`。
- `project.config.json` 已切换到正式 AppID `wx6968dcda3c5fb627`；开发版上传成功仍不等于微信审核或正式发布。

## 线上信息

- H5：`https://stardust.sale/campus-circle/`
- 健康检查：`https://stardust.sale/campus-circle/api/health`
- 本次接管未修改或部署线上环境；当前沙箱 DNS 无法解析该域名，因此线上状态尚未在本机复核。

## 2026-08-05 当前线上运行基线

- H5 已部署至 `https://stardust.sale/campus-circle/`，运营后台入口为 `/campus-circle/admin/`；API 健康检查和就绪检查均成功，PM2 进程 `stardust-campus-circle-api` 在线。
- 微信体验版 `0.1.7` 已于 2026-08-19 上传到 AppID `wx6968dcda3c5fb627`，微信 CLI 确认上传成功，包体 1,117,864 字节。包含独立 PNG 图标资源，主包原始文件总量约 1.82 MiB，并已通过 30 个页面、5 个底部导航资源与多多快递入口的自动检查；仍需公众平台提交审核并在审核通过后发布。
- 快递中的多多入口为 `https://mdkd.pinduoduo.com/weixin/pdd-packages`。它只能打开官方页面，不能读取包裹数据；第三方域名或公众号身份授权被微信拦截时，必须由多多提供授权的小程序跳转或业务域名方案，项目不能绕过验证。
- 静态站已添加 `X-Content-Type-Options`、`X-Frame-Options` 与 `Referrer-Policy` 响应头；Nginx 配置已备份并验证语法、重载、H5、后台和 API 正常。历史重复 Nginx 站点备份已移到可恢复的 `/etc/nginx/backups/sites-enabled-duplicates-20260805-045809/`，消除了重复 `server_name` 警告。
- API 生产环境已明确设置 `NODE_ENV=production`、`TRUST_PROXY=true` 和 `COMMUNITY_AUTO_PUBLISH=false`。修改前 `.env` 已备份，生产预检通过，仅重启 `stardust-campus-circle-api`，重启后 PM2、`/health`、`/ready` 均正常。
- 线上仍保持 `CHECKOUT_MODE=offline`、未配置通知网关和 OCR。收费/付费发布、自动通知和课表图片识别均不能对外承诺；具体上线门槛见 `docs/RELEASE-CHECKLIST.md`。
- 2026-08-13 已生成并验证 PostgreSQL custom-format 备份与上传目录归档，已配置每日 03:42 独立备份，不自动删除历史备份。最新备份已恢复到带安全前缀的临时数据库，成功检查 54 张公共表与迁移表，演练后临时库数量为 0。根分区当前使用率已达 97%，是上线前的明确运维风险，需先扩容或按归属安全清理。
- 2026-08-13 H5 已以不删除服务器旧文件的方式更新，部署前静态站备份位于 `/home/ubuntu/campus-circle-backups/20260813-003133-h5-static/`；首页、后台、API 健康/就绪接口均返回 200，Nginx 语法通过，其他静态内容保留。
- 备份已复制到本机 `/Users/a/Documents/stardust-campus-circle-offsite-backups/20260813/` 并生成 SHA-256 校验，避免只在单台服务器保留备份。
- 上传接口已加固：文件扩展名不再信任用户文件名，由服务端按 MIME 类型固定为 `.jpg` / `.png` / `.webp`，并校验文件头。线上回归确认 PNG 内容即使伪装 `.html` 文件名也只会保存为 `.png`，伪造图片返回 400，测试用户和文件已清理。
- 运营后台已配置高强度随机引导令牌，本机唯一明文副本位于 `/Users/a/Documents/stardust-campus-circle-offsite-backups/admin-bootstrap-token.txt`（权限 `600`）；线上已验证可访问唐山学院三个校区的管理、内容、财务和审计功能。正式分配用户管理员后应轮换或关闭引导令牌。
- 生产就绪度审计显示：三个校区当前管理员、运营联系方式、真实商品、未来菜单和公告数均为 0；`CHECKOUT_MODE=offline`，通知网关和 OCR 未配置。因此当前是可真机验收的体验版，不能对全校宣布正式营业。

## 2026-08-23 论坛与榜单灰度版本

- 校园圈新增“我的发布”、审核状态、撤回、稳定详情加载、评论删除/举报与失败重试；发帖进入人工审核时明确显示“已提交审核”，不再误报为已公开。
- 校园榜单补齐学生投稿撤回，并通过迁移 `014_community_ranking_launch.sql` 修复运营下架状态约束；后台可同时管理待审和已公开帖子/地点、处理评论举报并暂停异常校区账号。
- 校园服务本期保持 `CHECKOUT_MODE=offline`；兼职付费发布和午餐继续支付入口均关闭，不会向同学拉起微信支付。
- 一次性 PostgreSQL 16 数据库从空库执行迁移 001–014 成功，9 组 API 集成场景全部通过；TypeScript、服务端语法/单测、微信小程序与 H5 构建均通过，小程序主包 1.829 MiB。
- H5、运营后台、API 与迁移 014 已部署；PM2 仅重启 `stardust-campus-circle-api`，Nginx 语法检查与重载成功。部署前完整备份位于 `/home/ubuntu/campus-circle-backups/20260822T205456Z-forum-rankings-launch/`。
- 微信开发版本 `0.1.8` 已上传至 AppID `wx6968dcda3c5fb627`，包体 1,128,224 字节。它可用于体验成员灰度，但尚未完成公众平台审核与正式发布。

## 2026-08-23 摩卡 Pro 榜单内容版本

- “生活榜”新增 `摩卡 Pro 美发（唐山吾悦广场店）`，按唐山学院三个校区分别建立独立数据行，初始点赞均为 0，避免跨校区热度互相影响。
- 门店详情使用运营方提供的真实发型作品图，新增门店头像、服务标签、三张作品预览和来源说明；未核实具体楼层、电话、营业时间和价格，因此页面不编造这些信息，并明确提示以商场导视及到店沟通为准。
- 为保护顾客隐私，公开素材避开了可识别顾客正脸；作品图仅用于该门店在校园榜单内的展示。
- PostgreSQL 空库迁移 `001` 至 `015` 成功，9 组 API 集成测试全部通过；TypeScript、服务端检查/单测、微信小程序和 H5 构建均通过，390×844 榜单详情视觉回归无横向溢出。
- H5、API 源码与迁移 `015_add_mocha_pro_ranking_place.sql` 已部署；Nginx 语法检查、重载、校园圈 HTTPS 与既有主要站点检查通过，PM2 进程保持原 PID 在线。部署前完整备份位于 `/home/ubuntu/campus-circle-backups/20260823T055025Z-mocha-pro/`，数据库归档共 466 个 TOC 条目。
- 微信开发版本 `0.1.9` 已上传至 AppID `wx6968dcda3c5fb627`，上传包 1,135,448 字节。它尚未提交或通过微信审核，也不是正式发布版本。

## 2026-08-23 微信端 UI 与关键交互细化版本

- 公共高校头部移除仿微信胶囊和重复品牌区，改为单行“当前高校 / 校区 / 切换高校”信息栏；学校切换仍必须在独立页面点击“确认切换”，不会误触后立即切校。
- 校园服务首屏缩短概览区与常用服务卡片，12 项完整服务改为大字号单行卡片，减少两列小卡造成的拥挤和文字截断。
- 校园榜单统一回蓝白主色，放大地点图片和文字区域，支持两行门店名与推荐说明；“我的”身份卡同步压缩首屏占用。
- 快递页将原来会直接修改状态的“出示取件码”拆成“查看并复制取件码”和二次确认的“确认已取件”，并明确区分待取和历史记录；多多入口仍只是官方页面指引，不能读取第三方包裹数据。
- 校园外卖补齐购物车结算按钮的明确事件和“我的”返回主小程序个人页；跑腿示例中的学校名称恢复为唐山学院语境。
- TypeScript、服务端语法与 19 项单元测试通过；全新 PostgreSQL 16 数据库执行迁移 001–015 后，28 项单元/集成测试全部通过；微信小程序和 H5 构建通过，小程序主包原始文件 1.876 MiB，30 个页面与 5 个底部导航资源检查通过；390×844 首页与 10 个校园服务页面无横向溢出。
- 二手闲置页新增真实搜索、四类筛选、收藏入口和电动车价格排序，发布表单补齐标题、描述、价格及实拍图校验；微信开发版本 `0.2.1` 已上传至 AppID `wx6968dcda3c5fb627`，微信 CLI 确认上传成功，上传包 1,137,806 字节。它仍只是开发/体验版本，尚未提交审核或正式发布；本轮也未部署 H5、API 或数据库到线上。

## 2026-08-24 代取快递便捷闭环（尚未部署）

- “跑腿代取”改为三步快捷发布：选择代取/帮送、填写取件点和送达位置，常用地点、时效与酬金按高校和校区保存在本机，下次直接复用；备注为选填，不要求用户重复填写标题。
- 页面明确禁止在公开需求中填写取件码、手机号和宿舍房间号，接单后通过现有校内会话私下沟通；酬金保持线下结算，平台不代收、不伪造支付。
- 接单后补齐去沟通、跑腿同学确认送达、发布人确认收到、双方确认进度和误接后放回任务大厅；放回大厅会关闭当前接单人的旧会话，且双方任一方开始确认交接后不能再放弃。
- 新需求增加每小时发布频率限制、0～100 元酬金校验和取送地点不同校验，继续沿用高校/校区隔离、内容安全、审计日志及聊天举报机制。
- TypeScript、服务端语法、21 项单元测试、微信小程序与 H5 构建通过；一次性 PostgreSQL 16 空库执行迁移 001～016 后，覆盖发布、沟通、接单、放弃、重新接单和双方完成的集成场景通过。390×844 页面与发布表单无横向溢出，10 个校园服务页面回归通过；本轮未部署 API/H5，也未上传微信体验版。

## 2026-07-31 本地后端完善（尚未部署）

- 新增不可变迁移 `007_rankings_and_profile_summary.sql`：校园榜单地点、点赞、举报及人工审核数据表，并按高校/校区隔离。
- 新增榜单公开接口与运营审核接口；同学投稿固定进入 `pending`，审核通过后才公开。
- `GET /v1/me/summary` 已返回真实帖子、消息、课表、二手记录数量与最近活动。
- 榜单投稿、点赞、举报、“我的”页面和运营后台均已接入真实 API，不再使用本机榜单存储模拟。
- 使用一次性本地 PostgreSQL 16 实跑全部迁移和接口测试，5 组集成测试通过；跨用户、跨校区、审核和举报闭环均已覆盖。
- `npm run check` 通过，微信开发者工具镜像目录已同步；本轮未修改线上数据库、API、静态站或 PM2 服务。

## 2026-07-31 多多快递第一阶段接入（尚未部署）

- 新增小程序页面 `pages/pdd-express/index`，通过 `web-view` 打开多多快递官方取件页。
- 快递动态页面新增“多多快递取件”入口；首页待取数量不再写死，改为读取本项目现有快递 API。
- 多多网页未向本项目提供包裹数据 API 或 `postMessage` 回调，因此当前只负责打开官方取件页，不能把页面里的包裹和取件码自动同步进本项目。
- 正式小程序发布仍需真实企业 AppID，并由拼多多配合将 `mdkd.pinduoduo.com` 配置为本小程序业务域名；开发工具里的 `urlCheck: false` 不能替代正式配置。

## 2026-07-31 DeepSeek 与课表一键导入（尚未部署）

- 校园助手的可选模型服务已统一为 DeepSeek，环境变量更名为 `DEEPSEEK_API_BASE`、`DEEPSEEK_API_KEY`、`DEEPSEEK_MODEL`；前端会显示答案来源，服务不可用时只提供已核验的本地导航提示。
- 课表页新增“选择课表图片，一键识别”：整图先由强视觉模型完整读取表格，再由 DeepSeek-OCR 分栏和按单元格精读兜底，最后由 DeepSeek 文本模型生成课程 JSON；用户可逐门修改或删除，再批量去重保存到 `user_schedule_entries`。
- DeepSeek 官方文本 API 不接收图片，因此课表识别还需要配置 `DEEPSEEK_OCR_API_URL`、`DEEPSEEK_OCR_API_KEY`、`DEEPSEEK_OCR_MODEL`。强视觉模型通过 `SCHEDULE_VISION_API_URL`、`SCHEDULE_VISION_API_KEY`、`SCHEDULE_VISION_MODEL` 配置；地址和密钥留空时复用 OCR 服务，默认模型为 `Qwen/Qwen3-VL-235B-A22B-Instruct`。未配置时接口返回明确提示，不会伪造识别结果。
- 多多快递页面在微信开发者工具和桌面环境改为真机预览说明，避免再次进入公众号 OAuth 错误页；iOS、Android 和 HarmonyOS 真机仍打开官方取件网页。
- TypeScript、后端语法、后端单元测试、微信小程序构建、H5 构建均通过；开发者工具镜像已同步并实测课表入口、DeepSeek 助手和快递提示页。开发者工具仍有部署前已有的微信登录 `401`，需线上 AppID/AppSecret 匹配后解决。

## 2026-08-03 校区隔离、聊天、兼职付费发布与统一后台（尚未部署）

- 新增不可变迁移 `008_campus_admin_chat_jobs.sql`，为业务数据补齐 `campus_slug` 并回填现有数据；所有主要学生端、运营端查询与写入均按 `tenant_slug + campus_slug` 双重隔离。
- 管理角色扩展为平台、高校、校区、内容、财务和审计六类权限；新增用户校区成员关系、校区运营配置和管理操作审计。
- 二手闲置、学生发布兼职和失物招领已接入统一校内会话，支持消息已读、未读数、举报和后台冻结；禁止跨校区会话与资源访问。
- 新增学生兼职发布闭环：保存发布单、固定 ¥10 支付意图、支付成功后展示当前校区运营联系方式、人工沟通、审核发布、拒绝后排队原路退款。支付未配置时不会伪造成功或绕过审核。
- 新增统一 H5 运营后台 `admin/`：总览、内容审核、兼职发布、沟通举报、用户与权限、校区联系方式和审计日志；二手电动车微信咨询也读取当前校区后台配置，不再硬编码个人微信。
- 本地 PostgreSQL 16 从空库执行迁移 `001` 至 `008` 成功；6 组数据库/API 集成场景与 9 项后端单元测试全部通过（合计 15 项），覆盖跨校区不可见、会话、举报、兼职付费门槛、管理设置与统计。
- `npm run typecheck`、`npm --prefix server run check`、微信小程序构建、H5 构建和 29 页面结构校验均通过。微信小程序主包构建产物约 1.61 MB，已同步到 `/Users/a/WeChatProjects/stardust-campus-circle/dist/` 并在开发者工具打开校园服务、兼职和兼职发布页验证渲染。
- 本地运营后台已连接测试数据库验证高校/校区切换与联系方式页面。管理令牌比较和登录面板隐藏问题已在浏览器实测中修复。
- 本轮没有部署线上数据库、API、静态站，也没有重启线上 PM2。正式微信登录、AppID/AppSecret、内容审核服务及微信支付商户凭据仍是外部阻塞项。

## 2026-08-03 前后端业务对齐第二批（尚未部署）

- 新增不可变迁移 `009_home_dashboard.sql` 与 `010_service_commerce_and_conversations.sql`：首页公告/天气、校园商店商品/订单/库存、活动运营，以及跑腿和匿名匹配会话均按高校与校区隔离。
- 首页不再写死公告、天气、下一节课、日程数量和待取快递数量，统一读取 `GET /v1/home/summary`；没有真实数据时展示空状态。所有演示内容仅在本地显式开启 `stardust_enable_demo_content` 后出现。
- 校园水果店、花店和南院零食店已接入真实商品、库存和订单：同学提交订单时锁定并扣减库存，取消时恢复库存；运营后台可发布/上下架商品并推进订单状态，不会把未支付订单伪装成已支付。
- 校园活动已支持后台创建、启停与报名统计；跑腿支持接单前后校内聊天；匿名匹配接受问候后自动建立匿名会话，聊天中不会暴露用户昵称。
- 运营后台新增首页运营、服务运营和快递运营：可维护公告、天气、商品、服务订单、校园活动，并为当前校区指定用户录入待取快递；快递状态会同步到该用户首页。
- 使用一次性 PostgreSQL 16 空库执行迁移 `001` 至 `010` 成功，8 组端到端数据库/API 场景全部通过，覆盖库存扣减/恢复、订单状态机、活动启停、跑腿与匿名聊天、快递后台录入及跨校区隔离。
- 本轮仍未部署线上环境。多多快递仅能打开其官方页面，除非对方提供授权数据接口，否则不能自动抓取其中的包裹数据；正式微信登录、内容审核服务、微信支付和 DeepSeek/OCR 凭据仍需外部配置。

## 2026-08-04 候选人成品择优合并（尚未部署）

- 对前端 UI、后端工作台和全产品方向候选人成品完成干净构建与页面实测；只采用已验证且不改变业务信任边界的改动，没有直接覆盖主项目。
- 首页新增常用服务编辑入口、校园新鲜事标题和真实空状态；校园服务新增搜索清除与无结果恢复；校园圈空频道可直接发布；榜单补齐无地点引导；“我的”支持真实头像并保留失败降级；切换高校后返回原页面。
- 未采用尺寸失控的全局 CSS 放大、构建不可复现的提交、硬编码本地 API 地址，以及未通过数据库集成验证的候选后端代码。
- TypeScript、服务端语法、13 项服务端单元测试、微信小程序构建、H5 构建和 29 页面结构检查均通过；PostgreSQL 集成测试仍按现有配置跳过，H5 首屏 342 KiB 体积警告仍存在。
- 390×844 实测首页、服务筛选、校园圈空频道、榜单空状态、我的与切校返回均无横向溢出；构建产物已同步至 `/Users/a/WeChatProjects/stardust-campus-circle/dist/`，微信开发者工具自动热重载成功。
- 本轮未部署线上环境，未修改数据库、API、PM2、Nginx、上传目录或真实 AppID。开发者工具仍有既存微信登录 `401`，需正式 AppID/AppSecret 与线上服务配置匹配后解决。

## 2026-08-24 零食店与校园外卖独立子产品重设计（尚未部署）

- 南院零食店按独立小程序重做蓝白首屏：校区切换、品类筛选、可收起搜索、商品列表、购物车、自取/配送和订单提交已形成完整链路。
- 校园外卖按已选定的蓝白方案重排为“菜单状态—7 日预约—精选套餐—分类—热卖—固定结算栏”，并保留当日库存、幂等提交、自取/配送、联系信息校验、商家/骑手状态和订单记录。
- 校园商店订单新增商品分类、履约方式、联系人、手机号和配送地址字段；手机号按现有 PII 加密约定存储，学生端只回显授权范围内的数据，运营端才可查看履约联系方式。
- 390×844 实测零食店与校园外卖均无横向溢出；校园外卖完整走通加购、配送表单和订单提交，浏览器控制台无 error。
- 当前结算继续遵循 `CHECKOUT_MODE=offline`：页面会如实说明不发起在线扣款，未开通的支付不会被伪装成成功。
- 本轮未部署线上 API/H5，未执行生产数据库迁移，也未上传微信体验版。

## 2026-08-24 个人课表完整垂直切片（尚未部署）

- 个人课表按已选定的蓝白方案重做：大字标题、下一节课主卡、一张图导入、七日选择和当日日程；去掉了占据首屏的个人资料卡，手动添加收纳为辅助小入口。
- 课表图片导入支持选择图片、上传、识别、逐门修改/删除、批量去重保存；识别结果不直接覆盖原课表。
- 使用用户提供的真实课表作为本地演示基线，支持星期、节次、显式时间和周次解析；当前/下一节课会按时间自动更新。
- 云端课表不可用时会降级保存到当前设备并明确显示状态；图片识别仍需服务端 OCR/模型凭据，未配置时不伪造识别结果。
- 390×844 下完成真实文件选择、识别复核、批量导入、手动添加和日程展示回归，无横向溢出、控制台 error 为 0。
- TypeScript、课表/快递服务端聚焦测试与 H5 构建通过；本轮未部署 API/H5，未上传微信体验版。

## 2026-08-24 我的快递完整垂直切片（尚未部署）

- “我的快递”按蓝白独立子产品重做，主链路调整为复制完整到件短信后一键识别；普通短信在本机提取快递公司、取件码、取件点和运单号，不需要逐项录入。
- 识别结果会先进入可编辑复核卡，用户确认后才导入待取件；同一通知使用稳定事件标识去重，不会重复生成包裹。
- 待取件支持查看并复制取件码、二次确认已取件和历史留存；多多快递网页只作为官方备用入口，不宣称能够读取第三方包裹。
- 手机号和取件码继续按现有 PII 约定加密；学校驿站或物流服务商未配置签名回调时，手机号绑定显示“暂未开放”，不伪造自动同步。
- 修复驿站名称过度匹配：中通菜鸟驿站、顺丰丰巢柜和圆通服务站等常见通知均只保留干净地点名。
- 390 × 844 设计对照通过；TypeScript、服务端语法、12 项快递聚焦测试、H5、微信小程序构建和包体检查通过。微信包体 1.915 MiB，无检查错误，仅保留既有媒体总量优化建议。
- 本轮未部署线上 API/H5、未执行生产数据库迁移，也未上传微信体验版。

## 2026-08-24 二手闲置完整垂直切片（尚未部署）

- 二手闲置按已选蓝白设计稿完成二次完善：搜索、分类、最新/收藏、双列商品卡、真实空状态和发布入口形成一致层级。
- 修复无公开商品时大面积空白；空状态使用项目内真实素材和显式主按钮，搜索无结果、收藏为空、校区无商品分别回到正确流程。
- 发布页补齐分类、成色、价格、描述、实拍图和交易安全说明，提交固定进入人工审核；未审核内容不会直接公开。
- 商品新增完整详情层，买家可查看大图、成色、校区和描述后收藏或发起校内聊天；卖家可标记已交易和重新上架。
- 沿用高校/校区隔离、图片可信来源校验、内容安全、审核、审计和会话举报；不提供平台担保或未配置支付，明确当面验货、不提前转账。
- 390 × 844 浏览态和发布态无横向溢出；TypeScript、H5、微信小程序构建和包体检查通过，主包 1.924 MiB，无检查错误。
- 本轮未部署线上 API/H5、未执行生产数据库迁移，也未上传微信体验版。

## 2026-08-24 二手电动车完整垂直切片（尚未部署）

- 二手电动车按蓝白验车服务稿重做：首页支持真实车源大图、价格排序、续航和车龄筛选，空校区使用真实服务说明与发布入口，不注入虚假车辆、价格或登记信息。
- 新增不可变迁移 `020_market_vehicle_inspections.sql`，车辆发布结构化保存品牌、实际续航、电池年份和登记状态；发布仍需实拍图、内容安全检查和人工审核。
- 验车预约成为真实后端记录：买家选择未来 30 天日期、时段、校内地点和备注；车主可确认/完成，双方可取消，且拒绝预约自己车辆与重复待处理预约。
- 车源详情继续支持收藏、站内聊天、卖家标记已交易/重新上架和可选校区运营咨询；页面明确线下见面验车、确认车况再交易、平台不代收款。
- 车辆、预约、会话和审核均保持高校/校区隔离；运营后台同步展示品牌、续航、电池年份与登记状态。
- 390 × 844 车源空状态、发布表单和预约列表均无横向溢出；TypeScript、36 项服务端单元测试、H5、微信小程序构建与包体检查通过，主包 1.976 MiB。
- 本轮未部署线上 API/H5、未执行生产数据库迁移，也未上传微信体验版。

## 2026-08-24 兼职信息完整垂直切片（尚未部署）

- 兼职信息按蓝白设计稿补齐真实搜索、校内/周末/线上分类、全部/已核验/我的投递筛选和可恢复的无结果状态；不再保留只有外观的搜索框。
- 新增岗位详情层与独立报名确认卡，展示发布方、地点、岗位说明和防诈骗提示；学生可填写意向说明、提交、撤回，并在允许时与岗位发布者校内聊天。
- 只有当前高校/校区已审核岗位对外展示；学生发布的岗位需运营资质核验后才能公开，跨校区不可见。
- 当前继续关闭学生付费发布，页面明确“本期不收取发布费”，不拉起微信支付、不显示个人收款方式；后端历史付费发布能力没有对学生端开放。
- 390 × 844 无横向溢出；搜索、线上筛选和清除恢复已在外部 Chrome 实测。TypeScript、服务端语法、H5、微信小程序构建和包体检查通过，主包 1.933 MiB。
- 本轮未部署线上 API/H5、未执行生产数据库迁移，也未上传微信体验版。

## 2026-08-24 失物招领完整垂直切片（尚未部署）

- 失物招领按蓝白设计稿完成独立子产品化：寻物/招领双入口、真实搜索、类型筛选、我的发布、安全空状态、线索详情和底部导航形成一致主链路。
- 丢失和拾取会进入不同的专属发布页，内容分别保存为“寻物”/“招领”标签，并提示填写物品特征、时间、地点和实拍图。
- 线索继续按高校与校区隔离，发布经内容安全检查与审核后才公开；非发布者可通过站内会话沟通，发布者可标记已找回/已归还并移除公开线索。
- 页面明确要求证件只展示局部信息，禁止公开完整证件号、取件码、手机号和宿舍房间号，领取前需当面核对物品特征。
- 390 × 844 外部 Chrome 对照设计稿无横向溢出；TypeScript、服务端语法、H5、微信小程序构建和包体检查通过，主包 1.942 MiB。
- 本轮未部署线上 API/H5、未执行生产数据库迁移，也未上传微信体验版。

## 2026-08-24 校园活动完整垂直切片（尚未部署）

- 校园活动按橙蓝设计稿完成再设计：大图焦点位、七日日历、全部/社团/讲座/运动/志愿分类、全部/可报名/我的报名筛选和活动列表已形成可用闭环。
- 日期、类型和报名状态都是真实筛选；活动详情展示主办方、时间、地点、描述、已报/容量与到场提醒。
- 报名前会核对时间与地点并二次确认；已报名同学可取消并释放名额，满员活动禁止继续报名。
- 活动由当前校区运营审核并启用后展示，真实无数据时不注入虚假时间、名额和热度，使用可刷新的大图空状态。
- 390 × 844 外部 Chrome 验证无横向溢出；TypeScript、服务端语法、H5、微信小程序构建和包体检查通过，主包 1.950 MiB。
- 本轮未部署线上 API/H5、未执行生产数据库迁移，也未上传微信体验版。

## 2026-08-24 匿名匹配完整垂直切片（尚未部署）

- 匿名匹配按蓝白设计稿完成再设计：大图氛围、兴趣标签、候选卡、招呼收件箱、资料编辑和三项底部导航形成完整闭环。
- 候选按真实共同兴趣排序；同学可循环查看候选、发送自定义匿名招呼、同意或婉拒，只有双方同意后才建立匿名校内会话。
- 匹配资料支持兴趣快捷选择、自由兴趣、自我介绍、暂停和恢复；暂停后不再进入推荐，但保留历史招呼与会话。
- 页面持续禁止公开姓名、手机号、微信和宿舍号；候选、招呼和会话继续按高校与校区隔离。
- 390 × 844 外部 Chrome 验证主页面和资料页无横向溢出；TypeScript、服务端语法、H5、微信小程序构建和包体检查通过，主包 1.960 MiB。
- 本轮未部署线上 API/H5、未执行生产数据库迁移，也未上传微信体验版。

## 2026-08-24 校园花店完整垂直切片（尚未部署）

- 校园花店按蓝白花礼设计稿完成再设计：花束主视觉、毕业/生日/表白/日常分类、真实商品列表、固定预约栏和订单导航形成一致首屏。
- 无商品或本地 API 暂不可用时改为非技术化空状态与重试，不再显示 `Failed to fetch`，也不伪造花束、价格、库存、销量或送达承诺。
- 新增不可变迁移 `019_store_occasion_details.sql`；鲜花预约支持期望日期、期望时间、花卡留言、自取/校内送达、收花人、手机号和送达位置。
- 运营工作台订单卡可查看鲜花履约日期、时间和花卡留言；库存锁定、取消恢复、状态推进和高校/校区隔离沿用现有服务订单闭环。
- 当前继续采用离线预约模式，提交不会拉起未配置微信支付；价格与履约时间以校区运营确认结果为准。
- 390 × 844 外部 Chrome 对照无横向溢出；TypeScript、服务端语法、34 项单元测试、H5、微信小程序构建和包体检查通过，主包 1.969 MiB。
- 本轮未部署线上 API/H5、未执行生产数据库迁移，也未上传微信体验版。

## 2026-08-24 校园水果店完整垂直切片（尚未部署）

- 校园水果店按蓝白鲜果设计稿完成再设计：当季主视觉、当季/拼盘/宿舍装/饮品分类、真实商品卡、固定购物车与订单导航形成统一首屏。
- 校区切换入口、主图比例、标题字号、分类卡和真实空状态已重新排版；无商品或 API 暂不可用时不再留下大片空白或显示技术错误。
- 移除无法由后台证明的固定甜度星级和“30 分钟送达”，改为真实库存及“履约时间下单后确认”，不伪造商品质量和配送承诺。
- 分类、加购、库存限制、数量增减、自取/校内送达、联系方式、订单提交、状态查看、取消与库存恢复继续使用现有高校/校区隔离后端。
- 当前继续采用离线订单模式，不拉起未配置微信支付、不伪造扣款成功。
- 390 × 844 外部 Chrome 对照无横向溢出；TypeScript、服务端语法、H5、微信小程序构建和包体检查通过，主包 1.953 MiB。
- 本轮未部署线上 API/H5、未执行生产数据库迁移，也未上传微信体验版。

## 2026-08-24 校园圈 V4 完整垂直切片（尚未部署）

- 校园圈按选中的第二套蓝白方案重做：紧凑校区标题、五个大字号频道、轻量审核提示、内容流和悬浮发布入口构成论坛优先首屏，移除旧版占空间的品牌横幅和伪发现内容。
- 公共动态、我的发布、审核状态、撤回、点赞、详情、评论、评论删除、帖子与评论举报继续使用真实后端，并严格按高校和校区隔离。
- 发布页统一视觉并支持七类频道、最多三张压缩图片；新增不可变迁移 `021_community_post_images.sql`，后端验证可信上传地址、去重并保存 `image_urls`，同时保留首图兼容字段。
- 连接失败时不展示技术错误或虚假帖子，已有缓存继续可见；无缓存时仅显示一个明确重试状态，避免重复错误条。
- 390 × 844 外部 Chrome 验证无横向溢出；“我的发布”、频道筛选和悬浮发布入口均可用，发布页控制台 error 为 0。
- 本轮未部署线上 API/H5、未执行生产数据库迁移，也未上传微信体验版；部署前必须先执行数据库迁移。

## 2026-08-24 校园榜单 V4 完整垂直切片（尚未部署）

- 校园榜单按选定的社区投票流方案重做：紧凑校区头部、大字号生活/美食/玩乐榜、可信投票说明、连续排行、统一地点详情和地点上传形成完整闭环。
- 所有榜单地点均可打开详情；摩卡 Pro 使用项目内真实作品图展示服务项，页面不承诺未经核实的楼层、价格、折扣、联系方式或营业时间。
- 学生投稿必须填写名称、具体位置、推荐理由并上传一张真实照片，提交后按高校、校区和分类进入运营审核，不直接公开。
- 首次点赞前要求确认实际到访；正式榜单仅展示服务端真实点赞数。固定 48 赞只存在于显式标记的本地演示状态，生产 API 失败时不会注入假热度。
- 服务端修正榜单总数按分类统计，并强制校验投稿图片；390 × 844 外部 Chrome 实测分类切换、详情、上传和空表校验，无横向溢出、控制台 error 为 0。
- 本轮未部署线上 API/H5、未执行生产数据库迁移，也未上传微信体验版。

## 2026-08-24 消息中心 V4 完整垂直切片（尚未部署）

- 消息中心按任务型收件箱重做：沟通/通知双标签、未读摘要、全部/交易/互助/匿名筛选、业务头像、来源标签和连续会话列表形成清晰主链路。
- 订单进度和校园互动通知补齐真实业务跳转；校园互动通知由服务端返回帖子资源 ID，可直接进入对应帖子详情。
- 新增当前用户“全部已读”接口，仅更新当前高校、当前校区、当前用户收到的未读会话消息，继续保持租户与校区隔离。
- 网络失败和真实空状态不再混为一谈；失败时保留明确重载入口，不伪造同学、聊天内容、通知或未读数。
- 聊天页同步统一蓝白视觉，保留当前校区提示、匿名隐私保护、交易安全、举报、消息发送和内容安全检查。
- 390 × 844 外部 Chrome 实测沟通与通知切换均无横向溢出、控制台 error 为 0；本轮未部署线上 API/H5，也未上传微信体验版。

## 2026-08-24 我的页面 V4 完整垂直切片（尚未部署）

- “我的”按选定的校园生活仪表盘方案重做：身份与校区、帖子/消息/课表/交易快捷入口、下一节课、待取快递、今日行程、常用管理和账号管理形成完整个人中心。
- 接口失败时不再将未同步数据展示为 0 条或暂无包裹；远程信息显示“暂未同步”，本机已导入课表仍可正常使用。
- 消息与快递主链路已在外部 Chrome 实测跳转正确；隐私、数据管理和注销继续由规则页承接，并保留确认步骤。
- 修复 Taro H5 嵌套标签选择器未命中导致字号和布局偏大的问题，改为语义类名；390 × 844 无横向溢出，控制台 error 为 0。
- TypeScript 与 H5 构建通过；本轮未部署线上 API/H5，也未上传微信体验版。

## 2026-08-24 运营后台 V4 设计与未连接态验收（尚未部署）

- 后台按任务导向方案重构信息架构，九个原有模块完整保留，并归入工作台、运营、安全和系统四组。
- 未连接页改为紧凑连接表单与待审内容、服务订单、举报处置、校区开通检查四项锁定能力预览，不展示虚假数字。
- `Failed to fetch` 等技术错误改为中文诊断提示；未连接时导航和刷新操作均会被拦截并提示先连接。
- 顶部和侧栏持续显示高校、校区数据边界；连接后继续由真实上下文接口覆盖，支付功能保持关闭。
- 1440 × 1000 外部 Chrome 对照无横向溢出、控制台 error 为 0；JavaScript 语法与 H5 构建通过。
- 本轮没有启动生产后端、执行生产迁移、部署或上传微信体验版；完整连接态仍需在配置有效后台令牌和数据库的环境中做发布前验收。

## 2026-08-24 全量功能与包体最终回归（尚未部署）

- 12 个校园服务入口及外卖、订单、商家、骑手、运营等子页面已按独立功能产品完成 UI 与核心交互补齐；校园圈、榜单、消息、聊天、个人中心和运营后台同步完成统一蓝白体系收口。
- 新增 `docs/qa-all-pages.mjs`，直接读取微信构建产物的 `app.json`，在 390 × 844 下逐页核验全部 30 条路由；最终 30/30 可渲染、30/30 无横向溢出、控制台错误与页面异常均为 0。
- `docs/qa-services.mjs` 扩展到外卖、二手、跑腿、快递、兼职、活动、课表、匹配、水果、鲜花、零食、失物招领、电动车与服务聚合页，14 个场景全部通过。
- `docs/qa-mobile.mjs` 已适配最新首页、论坛内容流与悬浮发布入口；首页、校园圈、发布页和五项底部导航验证通过。
- 将 12 个高频服务图标替换为 88 × 88 WebP 构建资源，微信主包由 1.997 MiB 降至 1.795 MiB，197 个文件、30 个页面、最大单媒体 84.5 KiB，无包体错误，保留媒体总量迁移到 HTTPS 静态资源服务的优化建议。
- `npm run check` 全通过；服务端 37 项测试中 36 项通过、1 项需 PostgreSQL 的集成测试按环境条件跳过，无失败项。
- 支付继续保持关闭；本轮未部署线上 API/H5、未执行生产数据库迁移、未上传微信体验版或提交审核。
