# 星尘校园圈前端 UI 重设任务书

## 项目目标

这是一套面向唐山学院学生的微信小程序，包含首页、校园服务、校园圈、榜单、我的，以及外卖、二手、跑腿、失物招领、兼职、匿名匹配、水果店、花店、零食店、课表、快递等功能。

请在不改变现有业务逻辑、路由、API 契约和数据字段的前提下，重新设计全部前端 UI，使其达到可正式交付的校园生态产品水平。

## 不可改变的边界

- 保留所有现有页面、功能入口、路由、表单和状态。
- 保留现有 API 调用、高校/校区隔离、微信登录、内容审核、订单与聊天逻辑。
- 不写死演示数据，不伪造支付、快递、课表或订单结果。
- 必须同时适配微信小程序和 H5，以 390×844 为主要手机验收尺寸，不得横向溢出。
- 保留蓝白主色方向与 Stardust 品牌识别，但不要做成通用模板或幼稚卡通风。

## 设计重点

1. 首屏要舒展、大气，字号和点击区不得过小；减少一屏内过度堆叠的信息。
2. 建立统一设计系统：字号层级、间距、圆角、阴影、颜色、按钮、表单、标签和空状态必须一致。
3. 重做底部导航图标及选中/未选中状态，保证线条、尺寸和视觉重心统一。
4. 首页保留必要信息：学校/校区、公告、下节课、天气、快递、日程、校园服务和论坛信息，但重新整理层级与留白。
5. 校园服务不要只是相同卡片换颜色；每个业务页面要有符合其场景的首屏、导航、操作流和状态反馈。
6. 校园圈强化内容流可读性，保留悬浮发布；频道字号要舒适，不要被大面积装饰挤压。
7. 榜单突出地点投稿与点赞；“我的”页面要干净、有信任感，不要堆叠无效统计。
8. 为加载、无数据、失败、待审核、已下架、未开通、支付未配置等真实状态设计完整界面。
9. 运营后台也需要重设，但必须保留内容审核、商品/订单、快递、公告、用户权限、校区设置和审计日志全部功能。

## 交付要求

- 直接修改源码，不要只提供设计图。
- 优先完成全局设计系统和五个主导航页，再统一所有校园服务页与运营后台。
- 提交修改后的完整前端源码和一份简短修改说明。
- 至少通过 `npm run typecheck`、`npm run build:weapp`、`npm run check:weapp-package` 和 `npm run build:h5`。
- 如因环境无法构建，需明确列出未验证项，不得声称已通过。

## 包内结构

- `src/`：小程序/H5 前端源码与图片资源。
- `admin/`：运营后台前端。
- `config/`、`babel.config.js`、`project.config.json`：构建配置。
- `package.json`、`package-lock.json`：依赖与构建命令。
- `docs/api-contract.md`：现有 API 契约参考，不要自行改接口。
- `references/`：当前实现截图，仅用于理解现状，不要机械照搬。
