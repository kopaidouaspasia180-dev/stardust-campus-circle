# 星尘校园圈微信小程序导入说明

## 直接导入

1. 在本项目目录运行 `npm.cmd run build:weapp`。
2. 打开微信开发者工具，选择“导入项目”。
3. 项目目录选择本项目根目录，开发者工具会读取 `project.config.json`，小程序源码目录为 `dist/`。
4. 当前 `project.config.json` 使用 `touristappid`，适合本地预览。正式上传前，把它替换为你在微信公众平台获得的小程序 AppID。

## 正式发布前必须在微信公众平台完成

- 绑定企业主体与真实小程序 AppID。
- 配置服务类目、用户隐私保护指引和用户协议。
- 如果开放用户发帖、评论、图片上传，需要接入服务端账号体系、内容安全审核、举报、删除、封禁与日志留存。
- 如果接入天气、快递、课表或商家交易，需要配置 HTTPS 合法域名、对应服务协议和真实数据接口。
- 用户上传的图片不能长期保存临时文件路径，正式环境必须上传到合规对象存储并保存 HTTPS 地址。

## 当前首版已实现

- 多高校与校区切换框架。
- 今日校园信息区。
- 用户自定义 8 个常用功能。
- 校园服务搜索与分类。
- 校园圈频道筛选。
- 帖子发布、相册/相机选择图片、本地持久化。
- 帖子详情、点赞与评论。
- 首页连续校园信息流。
- 校园吃喝、玩乐、生活服务榜单。
- H5 与微信小程序双端构建。

当前帖子、点赞与评论保存在本机微信缓存中，便于产品首版体验；多用户正式运营需要接入独立后端。
